package com.example.myapplication.ui.theme

import androidx.compose.ui.graphics.Color

// --- Logo Brand Palette (Original Variable Names Kept!) ---

// Primary Brand Colors (From "FORMA" text)
val FormaBlue = Color(0xFF09378B)         // Deep, bold navy blue from logo
val FormaBlueDark = Color(0xFF052159)     // Darker navy for pressed states
val FormaAccentBlue = Color(0xFF4A76D6)   // Lighter, brighter navy for dark mode visibility

// Secondary Brand Colors (From "AI FITNESS" text & heartbeat)
val FormaTeal = Color(0xFF00B2A9)         // Vibrant teal/cyan from the logo
val FormaTealLight = Color(0xFF5CE1D9)    // Glowing cyan for dark mode highlights

// Backgrounds & Surfaces (Light)
val BackgroundLight = Color(0xFFF8FAFC)
val SurfaceLight = Color(0xFFFFFFFF)
val OutlineLight = Color(0xFFE2E8F0)

// Backgrounds & Surfaces (Dark)
val BackgroundDark = Color(0xFF0B1120)    // Deep rich navy-slate
val SurfaceDark = Color(0xFF16233B)       // Elevated navy-slate
val OutlineDark = Color(0xFF2C3E5D)

// Typography Colors
val TextPrimaryLight = Color(0xFF0F172A)
val TextSecondaryLight = Color(0xFF64748B)
val TextPrimaryDark = Color(0xFFF8FAFC)
val TextSecondaryDark = Color(0xFF94A3B8)

// Status & Accents
val ErrorRed = Color(0xFFEF4444)
val ErrorRedLight = Color(0xFFF87171)
val SuccessGreen = Color(0xFF22C55E)
val Gold = Color(0xFFFBBF24)

// Macros & Workouts
val ProteinColor = Color(0xFFE57373)
val CarbColor = Color(0xFF64B5F6)
val FatColor = Color(0xFFFFD54F)

val AmrapOrange = Color(0xFFFF9800)
val EmomPink = Color(0xFFE91E63)